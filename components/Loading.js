import React from 'react';
import Layout from './Layout';

export const Loading = () => {

    return (
        <Layout>
         <p className="mt-1 mr-2 text-gray-900 text-sm ">Cargando...</p>
         </Layout>
    );
};
